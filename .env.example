# Application Configuration
PORT=3000
API_PREFIX=api/v1

# JWT Authentication
JWT_SECRET=change_this_secret_in_production
JWT_EXPIRATION=24h

# PostgreSQL Database (FusionPBX)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=fusionpbx
DB_USER=fusionpbx
DB_PASSWORD=fusionpbx

# FreeSWITCH Event Socket Layer (ESL)
ESL_HOST=localhost
ESL_PORT=8021
ESL_PASSWORD=ClueCon

# Test Credentials (for /auth/login endpoint)
TEST_USERNAME=admin
TEST_PASSWORD=admin
